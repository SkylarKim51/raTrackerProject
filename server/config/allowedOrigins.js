const allowedOrigins = [
    //figure out what localhost i use in front end
    //for now localhost:5000 because that is what the post in first-react-app/src/pages/ uses
    'http://localhost:5000',
]

module.exports = allowedOrigins